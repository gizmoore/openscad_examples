# Openscad Examples

  - svg_to_2d.scad - convert SVG to 2D poly by using extrude followed
by projection.  Normally, the "import" command for SVG doesn't return
a path or polygon points.  Remember to use F6 to see the real 2D
representation.

  - Next example
